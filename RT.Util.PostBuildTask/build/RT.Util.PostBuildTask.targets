<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">  
	<UsingTask  
		TaskName="RT.Util.PostBuildTask.PostBuildRunner"  
		AssemblyFile="$(MSBuildThisFileDirectory)..\tools\RT.Util.PostBuildTask.dll">  
	</UsingTask>

	<Target Name="AfterBuild">
			<RT.Util.PostBuildTask.PostBuildRunner
				AssemblyPath="$(ProjectDir)@(MainAssembly)"
        SourcePath="$(ProjectDir)"
			/>
	</Target>
</Project> 